# Dependency registry

Every significant direct dependency records why it exists, where it may be
imported, its authoritative documentation, and compatibility/removal notes.
Transitive packages are not registered. Add an entry (and an ADR when the
choice is consequential) before adding a major dependency. Versions are exact;
`bun.lock` pins transitive versions. Registry versions verified 2026-09-20:
every workspace dependency is at the latest published release; the only
held-back majors are ESLint 10 (below) and TypeScript 7 (below).

## Runtime and toolchain

| Tool           | Version  | Purpose and allowed use                                                                    | Official documentation                                                                                        | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------------- | -------- | ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Bun            | `1.4.2`  | Only runtime, package manager, script runner, and unit/integration test runner             | [Docs](https://bun.sh/docs), [install](https://bun.sh/docs/installation)                                      | Pinned via `.bun-version`, `engines`, `packageManager`. Never use npm/pnpm/yarn. Commit `bun.lock`.                                                                                                                                                                                                                                                                                                                                                                                   |
| Turborepo      | `2.11.2` | Task graph, caching, parallel orchestration; never a package manager                       | [Docs](https://turborepo.dev/docs), [reference](https://turborepo.dev/docs/reference)                         | `bun --env-file=.env run turbo …` is the supported local invocation for env-dependent tasks; turbo runs in strict env mode.                                                                                                                                                                                                                                                                                                                                                           |
| TypeScript     | `6.0.3`  | Strict typing everywhere; shared config in `@daisy/typescript-config`                      | [TSConfig reference](https://www.typescriptlang.org/tsconfig/)                                                | TS 7.0.2 is outside `typescript-eslint` 8.70.0's supported range (`<6.1.0`); revisit after a peer-range bump.                                                                                                                                                                                                                                                                                                                                                                         |
| Prettier       | `3.9.8`  | Deterministic formatting; CI checks without rewriting files                                | [Install](https://prettier.io/docs/install.html)                                                              | Exact version for stable output across machines.                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ESLint         | `9.39.5` | Correctness and architecture rules only; no stylistic rules                                | [Getting started](https://eslint.org/docs/latest/use/getting-started)                                         | `typescript-eslint` 8.70.0; `eslint-config-next` scoped to `apps/web`. Boundary rules in `eslint.config.mjs` plus `scripts/check-boundaries.ts`. ESLint 10 is a deliberate hold: `eslint-plugin-react` 7.37.5 (latest, pulled by `eslint-config-next`) still calls `context.getFilename()`, removed in v10 — upgrade when jsx-eslint/eslint-plugin-react#3972 lands and `eslint-config-next` ships it.                                                                                |
| Knip           | `6.37.0` | Dead-code gate: unused files, exports, dependencies; root `bun run knip`                   | [Docs](https://knip.dev), [workspaces](https://knip.dev/features/monorepos-and-workspaces)                    | Runs under Bun (`bunx --bun knip`); config in `knip.jsonc`. Fails CI via the `checks` matrix (ADR 0013).                                                                                                                                                                                                                                                                                                                                                                              |
| jscpd          | `5.3.0`  | Copy-paste tripwire over first-party source; repo tooling only, root `bun run duplication` | [Docs](https://jscpd.dev), [repository](https://github.com/kucherenko/jscpd)                                  | Root devDependency; never imported by application or package code. jscpd 5 is a prebuilt Rust binary delivered through per-platform `optionalDependencies` (no install script, no native build); the launcher runs under Bun (`bunx --bun jscpd`). Config in `.jscpd.json`, grandfathered clones in `.jscpd-baseline.json` (ADR 0026). Removal: delete both files, the `duplication` script, its `check`/CI matrix/`scripts/evidence.ts` entries, and ADR 0026's rule in `AGENTS.md`. |
| RITEway        | `9.3.0`  | `riteway/bun` assert API on `bun:test`; declared per testing workspace                     | [Repository](https://github.com/ericelliott/riteway), [bun entry](https://github.com/ericelliott/riteway#bun) | Only the `riteway/bun` subpath is supported here. `expect().toThrow()` for exception paths only (ADR 0014).                                                                                                                                                                                                                                                                                                                                                                           |
| Docker Compose | current  | Local PostgreSQL/Redis only; app processes never run in Compose                            | [File reference](https://docs.docker.com/compose/compose-file/)                                               | Loopback-only ports; Postgres host port is 15432 to avoid common host installs.                                                                                                                                                                                                                                                                                                                                                                                                       |
| GitHub Actions | current  | CI: frozen install, checks, integration with real services, browser e2e                    | [Docs](https://docs.github.com/en/actions/get-started)                                                        | Pinned major action versions; Dependabot proposes upgrades.                                                                                                                                                                                                                                                                                                                                                                                                                           |

## Application

| Dependency                         | Version    | Purpose and allowed boundary                                                                   | Official documentation                                                                                                      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------------------------------- | ---------- | ---------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `next`                             | `16.3.5`   | Application delivery layer in `apps/web` only                                                  | [Docs](https://nextjs.org/docs)                                                                                             | Version-matched docs ship in `apps/web/node_modules/next/dist/docs/`; read before Next work. `proxy.ts` (not `middleware.ts`) is current.                                                                                                                                                                                                                                                                                                                                               |
| `react` / `react-dom`              | `19.3.0`   | UI rendering in `apps/web` only                                                                | [Learn](https://react.dev/learn)                                                                                            | Never imported by domain, protocol, or infrastructure packages.                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `zod`                              | `4.6.5`    | Runtime validation at trust boundaries: env, protocol, HTTP                                    | [Docs](https://zod.dev), [basics](https://zod.dev/basics)                                                                   | Zod 4 top-level APIs (`z.iso.datetime()`, `strictObject`). Parse once at boundaries; do not re-validate internal calls. Identifier shape is validated with a cuid2 regex (ADR 0018).                                                                                                                                                                                                                                                                                                    |
| `tailwindcss`                      | `4.3.3`    | Utility CSS compiler, configured only in CSS (`globals.css`); no JS config file                | [Docs](https://tailwindcss.com/docs), [theme](https://tailwindcss.com/docs/theme)                                           | ADR 0028. Default theme namespaces are reset so only Daisy tokens generate utilities. `apps/web` only.                                                                                                                                                                                                                                                                                                                                                                                  |
| `@tailwindcss/postcss`             | `4.3.3`    | The single PostCSS plugin that runs Tailwind in the Next build (`apps/web/postcss.config.mjs`) | [Next.js guide](https://tailwindcss.com/docs/installation/framework-guides/nextjs)                                          | Pinned to the `tailwindcss` version. Build-time stylesheet only; no runtime injection (CSP).                                                                                                                                                                                                                                                                                                                                                                                            |
| `prettier-plugin-tailwindcss`      | `0.8.1`    | Sorts class lists (`tailwindStylesheet` points at `globals.css`); root devDependency           | [Repository](https://github.com/tailwindlabs/prettier-plugin-tailwindcss)                                                   | Must load last in the Prettier plugin list. `format:check` fails on unsorted classes.                                                                                                                                                                                                                                                                                                                                                                                                   |
| `eslint-plugin-better-tailwindcss` | `4.7.0`    | Flat-config lint: unknown, conflicting, duplicate and restricted classes; root devDependency   | [Repository](https://github.com/schoero/eslint-plugin-better-tailwindcss)                                                   | Tailwind v4 and ESLint 9 flat config supported. Reads the theme from `globals.css`.                                                                                                                                                                                                                                                                                                                                                                                                     |
| `@types/node`                      | `26.6.2`   | Node type definitions required by Next.js typegen and build                                    | [DefinitelyTyped](https://github.com/DefinitelyTyped/DefinitelyTyped)                                                       | Declared in `apps/web` so CI resolves it inside the repo; the base tsconfig still limits global `types` to `["bun"]`.                                                                                                                                                                                                                                                                                                                                                                   |
| `@playwright/test`                 | `1.63.0`   | Browser E2E in `apps/web/e2e` only                                                             | [Intro](https://playwright.dev/docs/intro)                                                                                  | Runs under Node 24 LTS (upstream-supported driver runtime). Bun stays the only package/application runtime.                                                                                                                                                                                                                                                                                                                                                                             |
| `@axe-core/playwright`             | `4.13.0`   | Automated accessibility scans in `apps/web/e2e/accessibility.e2e.ts` only                      | [Repository](https://github.com/dequelabs/axe-core-npm/tree/develop/packages/playwright)                                    | Wraps `axe-core` for Playwright pages (`AxeBuilder({ page }).analyze()`). Only serious/critical findings gate the suite (AUTH-6.6); moderate/minor are reported, not blocking. Devdependency of `apps/web`.                                                                                                                                                                                                                                                                             |
| `better-auth`                      | `1.7.5`    | Server authentication framework, configured in `apps/web` only                                 | [Docs](https://better-auth.com/docs), [Next integration](https://better-auth.com/docs/integrations/next)                    | Magic link + passkey only; emailAndPassword, OAuth and password reset disabled. Server composition is lazy and resource-injected (ADR 0017). Installed with its first consumer (AUTH-3.1); registry-verified 2026-09-20.                                                                                                                                                                                                                                                                |
| `@better-auth/passkey`             | `1.7.5`    | WebAuthn passkey plugin + React client, `apps/web` only                                        | [Passkey plugin](https://better-auth.com/docs/plugins/passkey)                                                              | Pinned to the `better-auth` version. Real WebAuthn ceremonies; library owns challenge/token cryptography. Registration requires a discoverable credential with no attachment restriction (platform and roaming both enroll); `passkeyDeviceHintPlugin` adds the advisory WebAuthn `client-device` hint to both options responses, since the plugin has no hints option. The sign-in page arms conditional mediation (`signIn.passkey({ autoFill: true })`).                             |
| `@better-auth/drizzle-adapter`     | `1.7.5`    | Drizzle persistence adapter, constructed inside `@daisy/db` only                               | [Drizzle adapter](https://better-auth.com/docs/adapters/drizzle)                                                            | Built around the existing Bun SQL pool; only the opaque adapter capability leaves `@daisy/db`. Raw Drizzle access stays private to `db`.                                                                                                                                                                                                                                                                                                                                                |
| `@better-auth/memory-adapter`      | `1.7.5`    | In-memory auth adapter, `apps/web` unit test fixtures only                                     | [Adapters](https://better-auth.com/docs/concepts/database)                                                                  | devDependency pinned to the `better-auth` version; lets auth composition prove factory laziness and flows without live services. Never imported outside tests.                                                                                                                                                                                                                                                                                                                          |
| `@simplewebauthn/server`           | `13.3.1`   | Direct devDependency, `apps/web` integration test fixtures only                                | [Server docs](https://simplewebauthn.dev/docs/packages/server)                                                              | Pinned independently of `@better-auth/passkey`'s own resolved copy (`13.3.3`) because it is not a production dependency: it is the assertion authority the hand-built software WebAuthn authenticator test helper (`integration/webauthn-authenticator.ts`) proves its ES256/CBOR/attestation encoding against, so passkey ceremony tests exercise the real verification code the plugin runs, not a stub. Never imported outside `apps/web/integration`. Registry-verified 2026-09-22. |
| `resend`                           | `6.28.1`   | Official Resend webhook signature verifier, `apps/web` only (`Resend.webhooks.verify`)         | [Webhooks](https://resend.com/docs/webhooks/verify-webhooks-requests), [Node SDK](https://resend.com/docs/send-with-nodejs) | Registry-verified 2026-09-20. Message delivery uses a narrow injected `fetch`-based sender (bounded timeout, one idempotent retry), not the SDK; the SDK exists for the official raw-body verifier with a five-minute tolerance (ADR 0025). Never imported into the React graph.                                                                                                                                                                                                        |
| `@sentry/nextjs`                   | `11.0.0`   | Error tracking adapter, `apps/web/src/features/telemetry/` only (ADR 0037)                     | [Docs](https://docs.sentry.io/platforms/javascript/guides/nextjs/)                                                          | Adopted, not yet installed (PRIV-1 is docs-only; version is the npm registry `latest` tag read 2026-09-23, not an installed copy). PRIV-5 installs it with the adapter and the policy gate rejecting `@sentry/*` imports outside that module, and re-verifies the version at install time. Inert without `SENTRY_DSN`; `beforeSend` runs the pure `scrubEvent`, `sendDefaultPii: false`, no session replay.                                                                             |
| `posthog-js`                       | `1.434.11` | Product analytics client SDK, behind opt-in consent, `apps/web` client only (ADR 0037)         | [Docs](https://posthog.com/docs/libraries/js)                                                                               | Adopted, not yet installed (version is the npm registry `latest` tag read 2026-09-23). PRIV-6 installs it, re-verifies the version, and adds the adapter, consent banner and the policy gate rejecting `posthog-*` imports outside it. `opt_out_capturing_by_default: true` until opt-in; `person_profiles: 'identified_only'`.                                                                                                                                                         |
| `posthog-node`                     | `5.53.0`   | Product analytics server SDK, `apps/web` server only (ADR 0037)                                | [Docs](https://posthog.com/docs/libraries/node)                                                                             | Adopted, not yet installed (version is the npm registry `latest` tag read 2026-09-23). PRIV-6 installs it alongside `posthog-js` and re-verifies the version. Server-side `track` checks the request's consent cookie before sending; inert without `POSTHOG_API_KEY`. Also used for server-side person deletion (PRIV-4 vendor erasure).                                                                                                                                               |

## Domain and infrastructure

| Dependency             | Version   | Purpose and allowed boundary                          | Official documentation                                                                                                     | Notes                                                                                                                                                                                                                                                                                                                                   |
| ---------------------- | --------- | ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `@adobe/data`          | `0.10.19` | ECS storage inside the debate-engine adapter only     | [Repository](https://github.com/adobe/data), [API](https://adobe.github.io/data/)                                          | Pre-1.0: pin exact, inspect breaking changes every upgrade (ADR 0006). Vendor types never cross the engine boundary. A client-side attempt was rejected: archetype codegen requires `unsafe-eval`, conflicting with the strict CSP (ADR 0024).                                                                                          |
| `@paralleldrive/cuid2` | `3.3.0`   | Unguessable entity identifiers (ADR 0018)             | [Repository](https://github.com/paralleldrive/cuid2)                                                                       | Declared in `@daisy/clock`, `@daisy/db`, and `apps/web`. Default entropy in 3.3.0 is `crypto.getRandomValues`, silently falling back to `Math.random` only when Web Crypto is absent; pass an explicit OS CSPRNG via `init({ random })` for security-adjacent values as defence against that fallback (ADR 0018). Re-verify on upgrade. |
| `drizzle-orm`          | `0.45.2`  | Typed SQL and migrations in `@daisy/db` only          | [Bun SQL](https://orm.drizzle.team/docs/connect-bun-sql), [migrations](https://orm.drizzle.team/docs/drizzle-kit-generate) | Current website snippets advertise an RC; deliberately stay on stable 0.45.2. Rows are persistence, not domain.                                                                                                                                                                                                                         |
| `drizzle-kit`          | `0.31.10` | Schema diffing and studio, db development only        | [generate](https://orm.drizzle.team/docs/drizzle-kit-generate)                                                             | Generated SQL + metadata are committed and reviewed.                                                                                                                                                                                                                                                                                    |
| Bun SQL (`bun:sql`)    | runtime   | PostgreSQL driver, `@daisy/db` only                   | [SQL](https://bun.sh/docs/runtime/sql)                                                                                     | Native driver requires Bun in production. Node-only/serverless hosting needs a new adapter and ADR (ADR 0012).                                                                                                                                                                                                                          |
| Bun `RedisClient`      | runtime   | Ephemeral keys, `@daisy/redis` only                   | [Redis](https://bun.sh/docs/runtime/redis)                                                                                 | Redis >= 7.2. No Sentinel/Cluster; pub/sub experimental and unused. No second client without an ADR.                                                                                                                                                                                                                                    |
| `pino`                 | `10.3.1`  | Structured logging behind `@daisy/logger`             | [API](https://github.com/pinojs/pino/blob/main/docs/api.md), [redaction](https://getpino.io/#/docs/redaction)              | JSON to stdout; redaction is defense in depth, never an excuse to log secrets. No worker transports.                                                                                                                                                                                                                                    |
| `@opentelemetry/api`   | `1.9.1`   | Provider-neutral spans in `@daisy/observability` only | [JS instrumentation](https://opentelemetry.io/docs/languages/js/instrumentation/)                                          | API only; host/deployment installs provider and exporters. No SDK in the repo.                                                                                                                                                                                                                                                          |
| PostgreSQL             | `18`      | Durable authoritative state                           | [Manual](https://www.postgresql.org/docs/18/)                                                                              | Container volume root is `/var/lib/postgresql`. Major upgrades need backup/restore or pg_upgrade.                                                                                                                                                                                                                                       |
| Redis                  | `8`       | Ephemeral coordination only                           | [Docs](https://redis.io/docs/latest/)                                                                                      | Intentionally no durable volume; loss must be harmless. Never authoritative for results, ballots, ratings, identities.                                                                                                                                                                                                                  |

## Deliberately absent

- No second Redis client, no ORM besides Drizzle, no date library (`Date` +
  ISO strings with injected clocks), no validation library besides Zod, no
  error library (platform `Error.cause` plus `@daisy/errors`), no state
  manager besides the in-house observable UI store (ADR 0024 — client-side
  Adobe ECS rejected over the CSP conflict), no CSS framework besides token-locked Tailwind v4 (ADR 0028; no CSS Modules,
  no `tailwind.config.*`, no shadcn, no class-merge or variant library), and no
  OpenTelemetry SDK/exporters in-process.
- Add shared packages (`ui`, `testing`, `validation`) only when a second real
  consumer exists; do not create them speculatively.
