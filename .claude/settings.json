{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bun \"$CLAUDE_PROJECT_DIR/scripts/agent-guard.ts\" hook || { if [ \"$DAISY_AUTONOMOUS\" = 1 ]; then echo \"agent guard could not run; refusing this tool call\" >&2; exit 2; fi; exit 0; }",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
